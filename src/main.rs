use cpu::{Cpu, InstructionSet};

mod cpu;
mod memory;

fn main() {
    // let mut cpu = Cpu::new(vec![
    //     InstructionSet::Psh,
    //     InstructionSet::Direct(5),
    //     InstructionSet::Pop,
    //     InstructionSet::Psh,
    //     InstructionSet::Direct(6),
    // ]);
    // cpu.run();
}
